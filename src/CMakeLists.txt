cmake_minimum_required(VERSION 3.0)
project(src)
add_subdirectory(src)
add_library(srclib client.cpp client.h)
target_link_libraries(${PROJECT_NAME} srclib)
target_include_directories(src PUBLIC include/)